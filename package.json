{
  "name": "contenful-poc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=./build/out.js",
    "build:watch": "esbuild app.js --bundle --outfile=./build/out.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@contentful/rich-text-html-renderer": "^15.13.1",
    "esbuild": "^0.15.10"
  }
}